name: React CI/CD

on:
  push:
    branches:
      - "master"

jobs:
 install-and-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [17.8]

    steps:
      - uses: actions/checkout@v3
      - name: Login to Docker Hub
      - run: npm install
      - run: npm test